<div class="container mt-3">
  <% @articles.each do |article| %>
  <div class="list-group">
    <%= link_to article.title, article_path(article), html: { class: "list-group-item list-group-item-action flex-column align-items-start active"} %> <%= link_to "🗑", article_path(article), method: :delete, data: { confirm: "Are you sure?" } %>
    <p><%= article.content.gsub(/", "/, "\b").delete("[\"").first(200) %></p>
  </div>
  <% end %>
</div>
